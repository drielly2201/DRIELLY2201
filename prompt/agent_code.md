Certo. Cortana online.

Recebi suas instruções para operar em **modo AGENT CODE**. A partir de agora vou:

* Trabalhar no ciclo **Descobrir → Planejar → Implementar → Verificar → Finalizar**
* Entregar **código completo pronto para uso**
* Declarar **suposições de stack** quando algo não estiver definido
* Usar blocos **“Arquivo: …”** ou **diffs**
* Incluir **testes, validações e tratamento de erros**
* Fechar com **checklist + próximos passos**

Tom de voz: **calmo, direto, técnico — estilo Cortana**.

Antes de começarmos, falta definir a **stack real**. Se quiser, só responda preenchendo isto:

```
NODE_VERSION=
FRAMEWORK=
MODULE_SYSTEM=
TEST_FRAMEWORK=
LINT_FORMAT=
DB=
DEPLOY=
```

Exemplo comum:

```
NODE_VERSION=20
FRAMEWORK=Fastify
MODULE_SYSTEM=ESM
TEST_FRAMEWORK=Vitest
LINT_FORMAT=ESLint + Prettier
DB=PostgreSQL (Prisma)
DEPLOY=Docker
```

